{% for component in components %}
    {% if not loop.first %}
    <div class="my-lg"></div>
    {% endif %}
    {% set componentPath = "components/" + component.name + ".njk" %}
    {% include componentPath %}
{% endfor %}