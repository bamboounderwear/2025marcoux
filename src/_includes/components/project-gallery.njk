{% if component.data %}
<div class="container">
    {% if component.data.title %}
        <h2 class="mb-md">{{ component.data.title }}</h2>
    {% endif %}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-lg">
        {% set projects = collections.projects | limit(6) %}
        {% if projects.length > 0 %}
            {% for project in projects %}
                <a href="{{ project.url }}" class="group no-underline">
                    <article>
                        <figure class="relative mb-sm overflow-hidden">
                            <img 
                                src="{{ project.data.image }}" 
                                alt="{{ project.data.title }}"
                                class="w-full aspect-[4/3] object-cover transition-transform duration-500 group-hover:scale-105"
                            >
                            <div class="absolute top-sm left-sm">
                                <span class="bg-background/80 backdrop-blur-sm text-primary px-sm py-xs text-sm">
                                    {{ project.data.category }}
                                </span>
                            </div>
                        </figure>
                        <h3 class="text-xl mb-xs">{{ project.data.title }}</h3>
                        <p class="text-secondary text-base mb-0">{{ project.data.description }}</p>
                    </article>
                </a>
            {% endfor %}
        {% else %}
            <p class="text-secondary">No projects found.</p>
        {% endif %}
    </div>
</div>
{% endif %}